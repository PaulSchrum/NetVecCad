<Window x:Class="NetVecCad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetVecCad" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Canvas x:Name="DevCanvas" 
                ClipToBounds="True" 
                SnapsToDevicePixels="True" 
                Background="Ivory" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" 
                Margin="0,0,19.6,0.4" Loaded="DevCanvas_Loaded"></Canvas> 
    </Grid>
</Window>
